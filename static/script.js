$(document).ready(function () {
	var $headLine = $('#headline');
	$headLine.typed({
		strings: ["Murilo Bastos"],
		contentType: 'text',
		typeSpeed: 200,
		startDelay: 500,
		showCursor: true
	});
});